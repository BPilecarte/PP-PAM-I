<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AppTest.ViewModels"
             x:Class="AppTest.Views.SpellsView"
             Title="SpellsView"
             BackgroundColor="#F6F6F6">

    <ContentPage.BindingContext>
        <local:SpellsViewModel/>
    </ContentPage.BindingContext>


    <VerticalStackLayout>
        <Picker Title="Select Level" 
                ItemsSource="{Binding Levels}" 
                SelectedItem="{Binding SelectedLevel}" />

        <Picker Title="Select School" 
                ItemsSource="{Binding Schools}" 
                SelectedItem="{Binding SelectedSchool}" />

        <Button Text="Filter Spells" Command="{Binding FilterCommand}" />

        <CollectionView ItemsSource="{Binding Spells}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10">
                        <Label Text="{Binding Name}" FontAttributes="Bold" />
                        <Label Text="{Binding School}" />
                        <Label Text="{Binding Level}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>